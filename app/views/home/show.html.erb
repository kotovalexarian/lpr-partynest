<% content_for :top do %>
  <div class="jumbotron jumbotron-fluid text-center">
    <div class="container">
      <h1><%= translate :title %></h1>

      <p class="lead">
        <%= translate '.description' %>
      </p>

      <% if current_account&.own_membership_app.nil? %>
        <a role="button" class="btn btn-primary btn-lg" href="<%= new_membership_app_path %>">
          <%= translate '.join' %>
        </a>
      <% else %>
        <a role="button" class="btn btn-success btn-lg" href="<%= membership_app_path current_account.own_membership_app %>">
          <%= translate '.tracking' %>
        </a>
      <% end %>
    </div>
  </div>
<% end %>
