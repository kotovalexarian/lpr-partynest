<p><%= translate '.lead_text' %></p>

<%= link_to membership_app_url(
              guest_token: @membership_app.account.guest_token,
            ),
            membership_app_url(
              guest_token: @membership_app.account.guest_token,
            ) %>
